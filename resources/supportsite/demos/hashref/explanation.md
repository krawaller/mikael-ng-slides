Stolen from the Angular docs.

By putting `#box` on the input tag, we can reference that element elsewhere in the template.

The dummy binding to the `keyup` event is to make Angular update the bindings on events, otherwise it wouldn't.